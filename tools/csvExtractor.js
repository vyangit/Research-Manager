/**
 * Extracts a csv from a screenshot and saves it to the browser storage cache
 * @param img The screenshot to be processed into a csv
 * Returns an alert if csv is partially generated or if input is invalid
 */
function extractCsvFromScreenshot(img) {
    //TODO: Implement func
}